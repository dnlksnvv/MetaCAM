Place your Gerber test file here.

Default path used by the UI button:
  /test/sample.gtl

So put a file named:
  sample.gtl

Then rebuild/restart the docker frontend service so the file is included in the static bundle.
